[project]
name = "auto-ai-flow-worker"
version = "0.1.0"
description = "AI Agent Platform - Celery Worker"
requires-python = ">=3.10"
dependencies = [
    "celery[redis]>=5.3.0",
    "sqlalchemy>=2.0.0",
    "psycopg[binary]>=3.1.0",
]
